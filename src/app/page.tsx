import React from "react";
import Image from "next/image";
import { Navbar, MainImage } from "@/components";

const page = () => {
  return (
    <div>
      <Navbar/>
      <MainImage/>
    </div>
  );
};

export default page;
